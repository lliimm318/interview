*제가 짱짱 존경*

## SQL 중심적인 개발의 문제

### SQL의 의존적인 개발을 피하기 어렵다 !!!

나는 자바(객체지향) 언어로 개발을 한다. -> 객체를 관계형 DB에 관리 한다 -> **SQL !!!!! 꼭 꼭 꼭 필요**

반복되는 CRUD SQL을 짜고 짜고 또 짜고.....
SQL -> 자바 객체 / 자바 객체 -> SQL로 또 바꿔야 함

개발을 하는데 갑자기 새로운 컬럼이 추가가 되면?
-> 추가된 컬럼을 모든 쿼리를 다 추가해야 한다. 이러다 누락되면 버그 생기고 ㅠㅠㅠ

### 패러다임의 불일치 

*객체 ≒ 관계형 데이터베이스* (비슷하지만 달라요)

객체지향 프로그래밍은 추상화, 상속, 다형성, 캡슐화을 제공하지만 관계형 디비는 제공하지 않음 ㅠ

#### 객체를 관계형 DB에 저장
객체 -> SQL 변환 -> SQL > RDB

#### 객체 vs 관계 데이터베이스 차이

1. 상속

  
3. 연관관계
4. 데이터 타입
5. 데이터 식별 방법
   

